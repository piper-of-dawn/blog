{
  "buildCommand": "pip install -e . && mkdocs build",
  "outputDirectory": "site",
  "framework": "mkdocs",
  "installCommand": "pip install -e .",
  "devCommand": "mkdocs serve",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  }
}