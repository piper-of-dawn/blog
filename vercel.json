{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "site", "buildCommand": "npm run build:vercel" }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/$1" }]
}
