{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "uv venv .venv && uv pip install --python .venv/bin/python . 'mkdocstrings[python]>=0.26.1'",
  "buildCommand": "rm -rf .notes-source && git clone --depth 1 --branch master https://github.com/piper-of-dawn/KumarsNotes.git .notes-source && NOTES_SOURCE=.notes-source .venv/bin/python scripts/sync-notes.py && .venv/bin/mkdocs build --config-file pages/mkdocs.yml --site-dir public",
  "outputDirectory": "public"
}
