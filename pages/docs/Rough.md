```cpp
bool dock (vector<int> inp, int boats){
int is_avl[2];
for (size_t i = 0; i < inp.size();) {
int curr = inp[i];

switch curr:
case 1:
i+=2;
continue;
case 0:
if (is_avl(inp,i)) {
inp[i]==1;
}



}

bool is_avl (vector<int>& inp){
return inp[i-1]==0 && inp[i+1]==0;
}
